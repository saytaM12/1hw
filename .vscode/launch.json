{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/steg-decode",
            "args": ["img.ppm"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Makefile",
            "env": {
                "ASAN_OPTIONS": "detect_leaks=0"
            }
        }
    ]
}
